name: Security AI Scan
on:
  workflow_dispatch:
    inputs:
      mode:
        description: 'Scan Mode'
        required: false
        default: 'all'

jobs:
  security-scan:
          path: |
            ai_security_report.md
            vscode-scan-result.json
